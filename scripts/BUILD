# verify_install.py runs inside a throwaway venv that has the built wheel
# installed, not inside either resolve. It is declared here only so that fmt,
# lint and check cover it like the rest of the project's Python.
python_sources(
    name="lib_test",
    resolve="py-reqs-dev",
)
